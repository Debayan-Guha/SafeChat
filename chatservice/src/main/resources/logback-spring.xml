<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">
    
    <!-- ===== SPRING PROPERTIES ===== -->
    <springProperty scope="context" name="APP_ENV" source="spring.profiles.active" defaultValue="development"/>
    <springProperty scope="context" name="SERVICE_NAME" source="spring.application.name"/>
    
    <!-- ===== CONSOLE APPENDER (for immediate viewing) ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- ===== FILE APPENDER (for Promtail to read) ===== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${SERVICE_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/${SERVICE_NAME}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <root level="TRACE">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>  <!-- This writes logs to file, Promtail reads this -->
    </root>
</configuration>

<!--
Log Level Hierarchy (Most to Least Inclusive):
Level	Includes	What you see
TRACE	TRACE + DEBUG + INFO + WARN + ERROR	Everything
DEBUG	DEBUG + INFO + WARN + ERROR	Everything except TRACE
INFO	INFO + WARN + ERROR	Normal + warnings + errors
WARN	WARN + ERROR	Only warnings and errors
ERROR	ERROR	Only errors
-->